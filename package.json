{
  "name": "manufacturehub",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "db:migrate": "pnpm --filter @manufacturehub/api run db:migrate",
    "db:seed": "pnpm --filter @manufacturehub/api run db:seed",
    "db:generate": "pnpm --filter @manufacturehub/api run db:generate"
  },
  "devDependencies": {
    "turbo": "^2.3.0",
    "typescript": "^5.6.0"
  },
  "packageManager": "pnpm@9.15.0"
}
